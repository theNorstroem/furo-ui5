// DO NOT EDIT this file was generated with scripts/IntrinsicElements
import type ToolbarItemOverflowBehavior from "@ui5/webcomponents/dist/types/ToolbarItemOverflowBehavior.js";
import type * as React from "react";

export interface ToolbarSeparator extends React.HTMLAttributes<HTMLElement> {
  // no events defined.
  // properties

  // attributes

  /**
   * Property used to define the access of the item to the overflow Popover. If "NeverOverflow" option is set,
   * the item never goes in the Popover, if "AlwaysOverflow" - it never comes out of it.
   */
  overflowPriority?: ToolbarItemOverflowBehavior | keyof typeof ToolbarItemOverflowBehavior;

  /**
   * Defines if the toolbar overflow popup should close upon interaction with the item.
   * It will close by default.
   */
  preventOverflowClosing?: boolean;
}

declare module "react" {
  namespace JSX {
    interface IntrinsicElements {
      /**
       * A vertical divider that participates in the toolbar's overflow logic. For the standalone table toolbar use `furo-ui5-table-toolbar-separator` instead.
       *
       * ```html
       * <furo-ui5-toolbar>
       * <furo-ui5-toolbar-button text="Create"></furo-ui5-toolbar-button>
       * <furo-ui5-toolbar-separator></furo-ui5-toolbar-separator>
       * <furo-ui5-toolbar-button text="Delete"></furo-ui5-toolbar-button>
       * </furo-ui5-toolbar>
       * ```
       *
       * This is a pass-through wrapper around `furo-ui5-toolbar-separator`: the UI5 API (properties, events,
       * slots) is inherited unchanged and no data binding is added.
       *
       * ### Overview
       * The `furo-ui5-toolbar-separator` is an element, used for visual separation between two elements.
       * It takes no space in calculating toolbar items width.
       *
       * ### Overview
       * The `furo-ui5-toolbar-separator` is an element, used for visual separation between two elements.
       * It takes no space in calculating toolbar items width.
       *
       * Represents an abstract base class for items used in the `furo-ui5-toolbar`.
       *
       * Base class for all UI5 Web Components
       */
      "furo-ui5-toolbar-separator": ToolbarSeparator;
    }
  }
}
