// DO NOT EDIT this file was generated with scripts/IntrinsicElements
import * as React from "react";

import type { Money as FuroMoney } from "@/models/furo/type/Money";
import type { Money as GoogleMoney } from "@/models/google/type/Money";

export interface MoneyInput extends React.HTMLAttributes<HTMLElement> {
  // no events defined.
  // properties

  // undocumented
  amountValue?: string;

  /**
   * Use this to bind a model field by attribute.
   */
  model?: GoogleMoney | FuroMoney;

  // attributes

  // undocumented
  accessibleName?: string | undefined;

  // undocumented
  currencies?: string;

  // undocumented
  disabled?: boolean;

  // undocumented
  placeholder?: string | undefined;

  // undocumented
  readonly?: boolean;

  // undocumented
  required?: boolean;
}

declare module "react" {
  namespace JSX {
    interface IntrinsicElements {
      /**
       *
       * The `furo-ui5-money-input` binds a `google.type.Money` or `furo.type.Money` field to a
       * composition of an amount input (`furo-ui5-input` type Number) and a currency
       * `furo-ui5-combobox`. The amount round-trips through the model's `units` (INT64) and
       * `nanos` (INT32) fields; the currency is bound to the model's `currencyCode` field.
       *
       * You can offer a fixed currency list with the `currencies` attribute:
       *
       * ```html
       * <furo-ui5-money-input currencies="CHF,EUR,USD"></furo-ui5-money-input>
       * ```
       *
       * ## supported meta and constraints
       * - **readonly: true** — set the element to readonly
       * - **required: true** — mark the element as required
       */
      "furo-ui5-money-input": MoneyInput;
    }
  }
}
