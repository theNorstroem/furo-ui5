// DO NOT EDIT this file was generated with scripts/IntrinsicElements
import type { SideNavigationItemClickEventDetail, SideNavigationSelectionChangeEventDetail } from "@ui5/webcomponents-fiori/dist/SideNavigation.js";
import type * as React from "react";

export interface SideNavigation extends React.HTMLAttributes<HTMLElement> {
  "onitem-click"?: (d: CustomEvent<SideNavigationItemClickEventDetail>) => void;
  "onselection-change"?: (d: CustomEvent<SideNavigationSelectionChangeEventDetail>) => void;
  // properties

  // attributes

  /**
   * Defines the accessible ARIA name of the component.
   */
  accessibleName?: string | undefined;

  /**
   * Defines whether the `furo-ui5-side-navigation` is expanded or collapsed.
   *
   * **Note:** On small screens (screen width of 599px or less) the collapsed mode is not supported, and in
   * expanded mode the side navigation will take the whole width of the screen.
   * The `furo-ui5-side-navigation` component is intended to be used within a `furo-ui5-navigation-layout`
   * component to ensure proper responsive behavior. If you choose not to use the
   * `furo-ui5-navigation-layout`, you will need to implement the appropriate responsive patterns yourself,
   * particularly for smaller screens where the collapsed mode should not be used.
   */
  collapsed?: boolean;
}

declare module "react" {
  namespace JSX {
    interface IntrinsicElements {
      /**
       * The vertical application navigation. It has a main area (default slot) and a `fixedItems` area pinned to the bottom, and can be collapsed to icons only.
       *
       * ```html
       * <furo-ui5-side-navigation style="height:260px">
       * <furo-ui5-side-navigation-item text="Home" icon="home" selected></furo-ui5-side-navigation-item>
       * <furo-ui5-side-navigation-item text="Reports" icon="bar-chart">
       * <furo-ui5-side-navigation-sub-item text="Monthly"></furo-ui5-side-navigation-sub-item>
       * </furo-ui5-side-navigation-item>
       * <furo-ui5-side-navigation-item slot="fixedItems" text="Help" icon="sys-help"></furo-ui5-side-navigation-item>
       * </furo-ui5-side-navigation>
       * ```
       *
       * This is a pass-through wrapper around `furo-ui5-side-navigation`: the UI5 API (properties, events,
       * slots) is inherited unchanged and no data binding is added.
       *
       * ### Overview
       *
       * The `SideNavigation` is used as a standard menu in applications.
       * It consists of three containers: header (top-aligned), main navigation section (top-aligned) and the secondary section (bottom-aligned).
       *
       * - The header is meant for displaying user related information - profile data, avatar, etc.
       * - The main navigation section is related to the user's current work context.
       * - The secondary section is mostly used to link additional information that may be of interest (legal information, developer communities, external help, contact information and so on).
       *
       * ### Usage
       *
       * Use the available `furo-ui5-side-navigation-group`, `furo-ui5-side-navigation-item`
       * and `furo-ui5-side-navigation-sub-item` components to build your menu.
       * The items can consist of text only or an icon with text. The use or non-use of icons must be consistent for all items on one level.
       * You must not combine entries with and without icons on the same level. We strongly recommend that you do not use icons on the second level.
       *
       * The `furo-ui5-side-navigation` component is designed to be used within a `furo-ui5-navigation-layout` component to ensure an optimal user experience.
       *
       * Using it standalone may not match the intended design and functionality.
       * For example, the side navigation may not exhibit the correct behavior on smaller screens.
       * Additionally, the padding of the `furo-ui5-shellbar` will not match the padding of the side navigation.
       *
       * ### Keyboard Handling
       *
       * ### Fast Navigation
       * This component provides a build in fast navigation group which can be used via [F6] / [Shift] + [F6] / [Ctrl] + [Alt/Option] / [Down] or [Ctrl] + [Alt/Option] + [Up].
       * In order to use this functionality, you need to import the following module:
       * `import "@ui5/webcomponents-base/dist/features/F6Navigation.js"`
       *
       *
       * ### ES6 Module Import
       *
       * `import "@furo/ui5/dist/SideNavigation.js"`
       *
       * `import "@furo/ui5/dist/SideNavigationGroup.js";` (for `furo-ui5-side-navigation-group`)
       *
       * `import "@furo/ui5/dist/SideNavigationItem.js";` (for `furo-ui5-side-navigation-item`)
       *
       * `import "@furo/ui5/dist/SideNavigationSubItem.js";` (for `furo-ui5-side-navigation-sub-item`)
       *
       * ### Overview
       *
       * The `SideNavigation` is used as a standard menu in applications.
       * It consists of three containers: header (top-aligned), main navigation section (top-aligned) and the secondary section (bottom-aligned).
       *
       * - The header is meant for displaying user related information - profile data, avatar, etc.
       * - The main navigation section is related to the user's current work context.
       * - The secondary section is mostly used to link additional information that may be of interest (legal information, developer communities, external help, contact information and so on).
       *
       * ### Usage
       *
       * Use the available `furo-ui5-side-navigation-group`, `furo-ui5-side-navigation-item`
       * and `furo-ui5-side-navigation-sub-item` components to build your menu.
       * The items can consist of text only or an icon with text. The use or non-use of icons must be consistent for all items on one level.
       * You must not combine entries with and without icons on the same level. We strongly recommend that you do not use icons on the second level.
       *
       * The `furo-ui5-side-navigation` component is designed to be used within a `furo-ui5-navigation-layout` component to ensure an optimal user experience.
       *
       * Using it standalone may not match the intended design and functionality.
       * For example, the side navigation may not exhibit the correct behavior on smaller screens.
       * Additionally, the padding of the `furo-ui5-shellbar` will not match the padding of the side navigation.
       *
       * ### Keyboard Handling
       *
       * ### Fast Navigation
       * This component provides a build in fast navigation group which can be used via [F6] / [Shift] + [F6] / [Ctrl] + [Alt/Option] / [Down] or [Ctrl] + [Alt/Option] + [Up].
       * In order to use this functionality, you need to import the following module:
       * `import "@ui5/webcomponents-base/dist/features/F6Navigation.js"`
       *
       *
       * ### ES6 Module Import
       *
       * `import "@furo/ui5/dist/SideNavigation.js"`
       *
       * `import "@furo/ui5/dist/SideNavigationGroup.js";` (for `furo-ui5-side-navigation-group`)
       *
       * `import "@furo/ui5/dist/SideNavigationItem.js";` (for `furo-ui5-side-navigation-item`)
       *
       * `import "@furo/ui5/dist/SideNavigationSubItem.js";` (for `furo-ui5-side-navigation-sub-item`)
       *
       * Base class for all UI5 Web Components
       */
      "furo-ui5-side-navigation": SideNavigation;
    }
  }
}
