// DO NOT EDIT this file was generated with scripts/IntrinsicElements
import * as React from "react";

import type { NavigationNode } from "@/models/index.js";

export interface Tree extends React.HTMLAttributes<HTMLElement> {
  "onbranch-focused"?: (d: NavigationNode) => void;
  "onbranch-selected"?: (d: NavigationNode) => void;
  "onleaf-focused"?: (d: NavigationNode) => void;
  "onleaf-selected"?: (d: NavigationNode) => void;
  onname?: (d: CustomEvent) => void;
  "onnode-closed"?: (d: NavigationNode) => void;
  "onnode-focused"?: (d: NavigationNode) => void;
  "onnode-opened"?: (d: NavigationNode) => void;
  "onnode-selected"?: (d: NavigationNode) => void;
  "onnodes-collapsed"?: (d: NavigationNode) => void;
  "onnodes-expanded"?: (d: NavigationNode) => void;
  "onqp-change-requested"?: (d: object) => void;
  // properties

  // attributes

  /**
   * Maximal depth for the tree. `0` (default) means infinite.
   */
  depth?: number;

  /**
   * Maximal expand level relative to the current node when expanding recursively.
   */
  expandDepth?: number;

  /**
   * Indicates that the element is focused.
   */
  focused?: boolean;

  /**
   * Override the display name of the root object.
   */
  headerText?: string;

  /**
   * Hide the root node.
   */
  hideRootNode?: boolean;

  /**
   * Query param to watch. When set, `node-selected` is only fired via `selectById`; otherwise
   * a `qp-change-requested` event is emitted.
   */
  qp?: string;

  /**
   * Render the root node as a header section.
   */
  rootAsHeader?: boolean;

  /**
   * Indicates an active search. Use it to style items depending on this attribute.
   */
  _searchIsActive?: boolean;

  /**
   * Override the description of the root object.
   */
  secondaryText?: string;
}

declare module "react" {
  namespace JSX {
    interface IntrinsicElements {
      /**
       * `furo-furo-furo-ui5-tree`
       * Renders a recursive navigation tree from a `tree.Tree` or `tree.NavigationNode` model.
       *
       * Each node is rendered as a `furo-furo-furo-ui5-tree-item`. Expand / collapse, selection and focus
       * are propagated through the open-models event tree (`__broadcastEvent` / `__dispatchEvent`),
       * mirroring the behavior of the original `@furo/data` based implementation.
       *
       * ## Data signature
       *
       * ```yaml
       * - type: 'tree.Tree #Navigation tree type with recursive navigation nodes'
       * fields:
       * root: 'tree.NavigationNode:1 #Root node of the tree'
       * ```
       */
      "furo-ui5-tree": Tree;
    }
  }
}
