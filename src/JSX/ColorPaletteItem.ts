// DO NOT EDIT this file was generated with scripts/IntrinsicElements
import type { STRING, StringValue } from "@furo/open-models/";
import type { ColorPaletteItemNativeClickEventDetail } from "@ui5/webcomponents/dist/ColorPaletteItem.js";
import * as React from "react";

import type { FuroFatString } from "@/models/index.js";

export interface ColorPaletteItem extends React.HTMLAttributes<HTMLElement> {
  onclick?: (d: CustomEvent<ColorPaletteItemNativeClickEventDetail>) => void;
  // properties

  /**
   * Use this to bind a model field by attribute.
   */
  model?: STRING | FuroFatString | StringValue;

  // attributes

  /**
   * Defines if the component is selected.
   *
   * **Note:** Only one item must be selected per <code>furo-ui5-color-palette</code>.
   * If more than one item is defined as selected, the last one would be considered as the selected one.
   */
  selected?: boolean;

  /**
   * Defines the tooltip of the component. When not set, the color value is used as the tooltip.
   */
  tooltip?: string | undefined;

  /**
   * Defines the colour of the component.
   *
   * **Note:** The value should be a valid CSS color.
   */
  value?: string;
}

declare module "react" {
  namespace JSX {
    interface IntrinsicElements {
      /**
       * The `furo-ui5-color-palette-item` is a single color swatch, meant to be used inside a
       * `furo-ui5-color-palette` or `furo-ui5-color-palette-popover`.
       *
       * It extends the [SAP ui5 ColorPaletteItem element](https://ui5.github.io/webcomponents/components/ColorPalette/).
       *
       * You can bind any `string` type (a CSS color): `primitives.STRING`, the `furo.fat.String` type or the
       * `google.protobuf.StringValue` type. Display-only: the component reads the color from the model into
       * the inherited `value` property and never writes back.
       *
       * ### Overview
       *
       * The `furo-ui5-color-palette-item` component represents a color in the the `furo-ui5-color-palette`.
       *
       * ### Overview
       *
       * The `furo-ui5-color-palette-item` component represents a color in the the `furo-ui5-color-palette`.
       *
       * Base class for all UI5 Web Components
       */
      "furo-ui5-color-palette-item": ColorPaletteItem;
    }
  }
}
