// DO NOT EDIT this file was generated with scripts/IntrinsicElements
import type * as React from "react";

export interface ProductSwitchItem extends React.HTMLAttributes<HTMLElement> {
  onclick?: (d: CustomEvent) => void;
  // properties

  // attributes

  /**
   * Defines the icon to be displayed as a graphical element within the component.
   *
   * Example:
   *
   * `<furo-ui5-product-switch-item icon="palette">`
   *
   * See all the available icons in the [Icon Explorer](https://sdk.openui5.org/test-resources/sap/m/demokit/iconExplorer/webapp/index.html).
   */
  icon?: string | undefined;

  /**
   * Defines the subtitle of the component.
   */
  subtitleText?: string | undefined;

  /**
   * Defines a target where the `targetSrc` content must be open.
   *
   * Available options are:
   *
   * - `_self`
   * - `_top`
   * - `_blank`
   * - `_parent`
   * - `_search`
   *
   * **Note:** By default target will be open in the same frame as it was clicked.
   */
  target?: string | undefined;

  /**
   * Defines the component target URI. Supports standard hyperlink behavior.
   */
  targetSrc?: string | undefined;

  /**
   * Defines the title of the component.
   */
  titleText?: string | undefined;
}

declare module "react" {
  namespace JSX {
    interface IntrinsicElements {
      /**
       * One tile of a `furo-ui5-product-switch`, with a title, subtitle, icon and optional `target-src`.
       *
       * ```html
       * <furo-ui5-product-switch>
       * <furo-ui5-product-switch-item
       * title-text="Analytics"
       * subtitle-text="Reports and forecasts"
       * icon="bar-chart"
       * ></furo-ui5-product-switch-item>
       * </furo-ui5-product-switch>
       * ```
       *
       * This is a pass-through wrapper around `furo-ui5-product-switch-item`: the UI5 API (properties, events,
       * slots) is inherited unchanged and no data binding is added.
       *
       * ### Overview
       * The `furo-ui5-product-switch-item` web component represents the items displayed in the
       * `furo-ui5-product-switch` web component.
       *
       * **Note:** `furo-ui5-product-switch-item` is not supported when used outside of `furo-ui5-product-switch`.
       *
       * ### Keyboard Handling
       * The `furo-ui5-product-switch` provides advanced keyboard handling.
       * When focused, the user can use the following keyboard
       * shortcuts in order to perform a navigation:
       *
       * - [Space] / [Enter] or [Return] - Trigger `furo-ui5-click` event
       *
       * ### ES6 Module Import
       * `import "@furo/ui5/dist/ProductSwitchItem.js";`
       *
       * ### Overview
       * The `furo-ui5-product-switch-item` web component represents the items displayed in the
       * `furo-ui5-product-switch` web component.
       *
       * **Note:** `furo-ui5-product-switch-item` is not supported when used outside of `furo-ui5-product-switch`.
       *
       * ### Keyboard Handling
       * The `furo-ui5-product-switch` provides advanced keyboard handling.
       * When focused, the user can use the following keyboard
       * shortcuts in order to perform a navigation:
       *
       * - [Space] / [Enter] or [Return] - Trigger `furo-ui5-click` event
       *
       * ### ES6 Module Import
       * `import "@furo/ui5/dist/ProductSwitchItem.js";`
       *
       * Base class for all UI5 Web Components
       */
      "furo-ui5-product-switch-item": ProductSwitchItem;
    }
  }
}
