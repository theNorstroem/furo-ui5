// DO NOT EDIT this file was generated with scripts/IntrinsicElements
import type { STRING, StringValue } from "@furo/open-models/";
import type {
  BarcodeScannerDialogScanErrorEventDetail,
  BarcodeScannerDialogScanSuccessEventDetail,
} from "@ui5/webcomponents-fiori/dist/BarcodeScannerDialog.js";
import * as React from "react";

import type { FuroFatString } from "@/models/index.js";

export interface BarcodeScannerDialog extends React.HTMLAttributes<HTMLElement> {
  onclose?: (d: CustomEvent) => void;
  "onfuro-value-changed"?: (d: string) => void;
  "onscan-error"?: (d: CustomEvent<BarcodeScannerDialogScanErrorEventDetail>) => void;
  "onscan-success"?: (d: CustomEvent<BarcodeScannerDialogScanSuccessEventDetail>) => void;
  // properties

  // undocumented
  code?: string;

  /**
   * Use this to bind a model field by attribute.
   */
  model?: STRING | FuroFatString | StringValue;

  // attributes

  /**
   * Indicates whether the dialog is open.
   */
  open?: boolean;
}

declare module "react" {
  namespace JSX {
    interface IntrinsicElements {
      /**
       * The 'furo-furo-furo-ui5-barcode-scanner-dialog' component  provides barcode scanning functionality for all devices that
       * support the MediaDevices.getUserMedia() native API. Opening the dialog launches the device camera and scans for known barcode formats.
       * Internally, the component uses the zxing-js/library third party OSS. For a list of supported barcode formats, see the
       * zxing-js/library documentation. https://github.com/zxing-js/library
       *
       * It supports all features from the [SAP ui5 Barcode Scanner Dialog element](https://sap.github.io/furo-furo-ui5-webcomponents/playground/components/BarcodeScannerDialog/).
       *
       * You can bind any `string` type, like `furo.fat.String` type or the `google.protobuf.StringValue` type.
       *
       * ```html
       * <furo-furo-furo-ui5-button at-click=--openClicked>Open Scanner</furo-furo-furo-ui5-button>
       * <furo-furo-furo-ui5-barcode-scanner-dialog fn-show="--openClicked"
       * fn-bind-data="--dao(*.field)"></furo-furo-furo-ui5-barcode-scanner-dialog>
       *
       * ```
       *
       * ## Methods
       * **bindData(fieldNode)**
       * Bind an entity field. You can use the entity even when no data was received.
       *
       * When you use at-object-ready from a furo-data-object which emits a EntityNode, just bind the field with --entity(*.fields.fieldname)
       *
       * ### Overview
       *
       * The `BarcodeScannerDialog` component provides barcode scanning functionality for all devices that support the `MediaDevices.getUserMedia()` native API.
       * Opening the dialog launches the device camera and scans for known barcode formats.
       *
       * A `scanSuccess` event fires whenever a barcode is identified
       * and a `scanError` event fires when the scan failed (for example, due to missing permisions).
       *
       * Internally, the component  uses the zxing-js/library third party OSS.
       *
       * For a list of supported barcode formats, see the [zxing-js/library](https://github.com/zxing-js/library) documentation.
       *
       * ### Overview
       *
       * The `BarcodeScannerDialog` component provides barcode scanning functionality for all devices that support the `MediaDevices.getUserMedia()` native API.
       * Opening the dialog launches the device camera and scans for known barcode formats.
       *
       * A `scanSuccess` event fires whenever a barcode is identified
       * and a `scanError` event fires when the scan failed (for example, due to missing permisions).
       *
       * Internally, the component  uses the zxing-js/library third party OSS.
       *
       * For a list of supported barcode formats, see the [zxing-js/library](https://github.com/zxing-js/library) documentation.
       *
       * Base class for all UI5 Web Components
       */
      "furo-ui5-barcode-scanner-dialog": BarcodeScannerDialog;
    }
  }
}
