// DO NOT EDIT this file was generated with scripts/IntrinsicElements
import type { ShellBarAccessibilityAttributes } from "@ui5/webcomponents-fiori/dist/shellbar/ShellBarAccessibility.js";
import type {
  ShellBarContentItemVisibilityChangeEventDetail,
  ShellBarLogoClickEventDetail,
  ShellBarMenuItemClickEventDetail,
  ShellBarNotificationsClickEventDetail,
  ShellBarProductSwitchClickEventDetail,
  ShellBarProfileClickEventDetail,
  ShellBarSearchButtonEventDetail,
  ShellBarSearchFieldClearEventDetail,
  ShellBarSearchFieldToggleEventDetail,
} from "@ui5/webcomponents-fiori/dist/ShellBar.js";
import * as React from "react";

export interface ShellBar extends React.HTMLAttributes<HTMLElement> {
  "oncontent-item-visibility-change"?: (d: CustomEvent<ShellBarContentItemVisibilityChangeEventDetail>) => void;
  "onlogo-click"?: (d: CustomEvent<ShellBarLogoClickEventDetail>) => void;
  "onmenu-item-click"?: (d: CustomEvent<ShellBarMenuItemClickEventDetail>) => void;
  "onnotifications-click"?: (d: CustomEvent<ShellBarNotificationsClickEventDetail>) => void;
  "onproduct-switch-click"?: (d: CustomEvent<ShellBarProductSwitchClickEventDetail>) => void;
  "onprofile-click"?: (d: CustomEvent<ShellBarProfileClickEventDetail>) => void;
  "onsearch-button-click"?: (d: CustomEvent<ShellBarSearchButtonEventDetail>) => void;
  "onsearch-field-clear"?: (d: CustomEvent<ShellBarSearchFieldClearEventDetail>) => void;
  "onsearch-field-toggle"?: (d: CustomEvent<ShellBarSearchFieldToggleEventDetail>) => void;
  // properties

  // attributes

  /**
   * Defines additional accessibility attributes on different areas of the component.
   *
   * The accessibilityAttributes object has the following fields,
   * where each field is an object supporting one or more accessibility attributes:
   *
   * - **logo** - `logo.role` and `logo.name`.
   * - **notifications** - `notifications.expanded` and `notifications.hasPopup`.
   * - **profile** - `profile.expanded`, `profile.hasPopup` and `profile.name`.
   * - **product** - `product.expanded` and `product.hasPopup`.
   * - **search** - `search.hasPopup`.
   * - **overflow** - `overflow.expanded` and `overflow.hasPopup`.
   * - **branding** - `branding.name`.
   *
   * The accessibility attributes support the following values:
   *
   * - **role**: Defines the accessible ARIA role of the logo area.
   * Accepts the following string values: `button` or `link`.
   *
   * - **expanded**: Indicates whether the button, or another grouping element it controls,
   * is currently expanded or collapsed.
   * Accepts the following string values: `true` or `false`.
   *
   * - **hasPopup**: Indicates the availability and type of interactive popup element,
   * such as menu or dialog, that can be triggered by the button.
   *
   * Accepts the following string values: `dialog`, `grid`, `listbox`, `menu` or `tree`.
   * - **name**: Defines the accessible ARIA name of the area.
   * Accepts any string.
   */
  accessibilityAttributes?: ShellBarAccessibilityAttributes;

  /**
   * Disables the automatic search field expansion/collapse when the available space is not enough.
   *
   * **Note:** The `disableSearchCollapse` property is in an experimental state and is a subject to change.
   */
  disableSearchCollapse?: boolean;

  /**
   * Defines the visibility state of the search button.
   *
   * **Note:** The `hideSearchButton` property is in an experimental state and is a subject to change.
   */
  hideSearchButton?: boolean;

  /**
   * Returns the `logo` DOM ref.
   */
  logoDomRef?: HTMLElement | null;

  /**
   * Defines the `notificationsCount`,
   * displayed in the notification icon top-right corner.
   */
  notificationsCount?: string | undefined;

  /**
   * Returns the `notifications` icon DOM ref.
   */
  notificationsDomRef?: HTMLElement | null;

  /**
   * Returns the `overflow` icon DOM ref.
   */
  overflowDomRef?: HTMLElement | null;

  /**
   * Defines the `primaryTitle`.
   *
   * **Note:** The `primaryTitle` would be hidden on S screen size (less than approx. 700px).
   */
  primaryTitle?: string | undefined;

  /**
   * Returns the `product-switch` icon DOM ref.
   */
  productSwitchDomRef?: HTMLElement | null;

  /**
   * Returns the `profile` icon DOM ref.
   */
  profileDomRef?: HTMLElement | null;

  /**
   * Defines the `secondaryTitle`.
   *
   * **Note:** The `secondaryTitle` would be hidden on S and M screen sizes (less than approx. 1300px).
   */
  secondaryTitle?: string | undefined;

  /**
   * Drops a shadow below the shellbar
   */
  shadow?: boolean;

  /**
   * Defines, if the notification icon would be displayed.
   */
  showNotifications?: boolean;

  /**
   * Defines, if the product switch icon would be displayed.
   */
  showProductSwitch?: boolean;

  /**
   * Defines, if the Search Field would be displayed when there is a valid `searchField` slot.
   *
   * **Note:** By default the Search Field is not displayed.
   */
  showSearchField?: boolean;
}

declare module "react" {
  namespace JSX {
    interface IntrinsicElements {
      /**
       *
       *
       * ### Overview
       *
       * The `furo-furo-ui5-shellbar` is meant to serve as an application header
       * and includes numerous built-in features, such as: logo, profile image/icon, title, search field, notifications and so on.
       *
       * ### Stable DOM Refs
       *
       * You can use the following stable DOM refs for the `furo-furo-ui5-shellbar`:
       *
       * - logo
       * - notifications
       * - overflow
       * - profile
       * - product-switch
       *
       * ### Keyboard Handling
       *
       * #### Fast Navigation
       * This component provides a build in fast navigation group which can be used via [F6] / [Shift] + [F6] / [Ctrl] + [Alt/Option] / [Down] or [Ctrl] + [Alt/Option] + [Up].
       * In order to use this functionality, you need to import the following module:
       * `import "@ui5/webcomponents-base/dist/features/F6Navigation.js"`
       *
       * ### ES6 Module Import
       * `import "@furo/ui5/dist/ShellBar.js";`
       *
       * ### Overview
       *
       * The `furo-ui5-shellbar` is meant to serve as an application header
       * and includes numerous built-in features, such as: logo, profile image/icon, title, search field, notifications and so on.
       *
       * ### Stable DOM Refs
       *
       * You can use the following stable DOM refs for the `furo-ui5-shellbar`:
       *
       * - logo
       * - notifications
       * - overflow
       * - profile
       * - product-switch
       *
       * ### Keyboard Handling
       *
       * #### Fast Navigation
       * This component provides a build in fast navigation group which can be used via [F6] / [Shift] + [F6] / [Ctrl] + [Alt/Option] / [Down] or [Ctrl] + [Alt/Option] + [Up].
       * In order to use this functionality, you need to import the following module:
       * `import "@ui5/webcomponents-base/dist/features/F6Navigation.js"`
       *
       * ### ES6 Module Import
       * `import "@furo/ui5/dist/ShellBar.js";`
       *
       * Base class for all UI5 Web Components
       */
      "furo-ui5-shellbar": ShellBar;
    }
  }
}
