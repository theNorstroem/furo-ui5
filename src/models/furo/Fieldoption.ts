// Code generated by furo protoc-gen-open-models. DO NOT EDIT.
// protoc-gen-open-models version: ????

import { ANY, ARRAY, FieldNode, Registry, STRING, type IAny } from "@furo/open-models/dist/index";

/**
 * @interface IFieldoption
 *  Metas for a field
 */
export interface IFieldoption {
  /**
   *  Add flags for your field. This can be something like "searchable".
   *  //The flags can be used by generators, ui components,...
   */
  flags?: string[];
  /**
   *  a list with options, use furo.optionitem or your own
   */
  list?: IAny;
}

/**
 * @interface TFieldoption
 *  Metas for a field
 */
export interface TFieldoption {
  /**
   *  Add flags for your field. This can be something like "searchable".
   *  //The flags can be used by generators, ui components,...
   */
  flags?: string[];
  /**
   *  a list with options, use furo.optionitem or your own
   */
  list?: IAny;
}

/**
 * Fieldoption
 *  Metas for a field
 */
export class Fieldoption extends FieldNode {
  /**
   * Add flags for your field. This can be something like "searchable".
   * //The flags can be used by generators, ui components,...
   **/
  private _flags: ARRAY<STRING, string>;

  /**
   * a list with options, use furo.optionitem or your own
   **/
  private _list: ANY;

  public __defaultValues: IFieldoption;

  constructor(initData?: IFieldoption, parent?: FieldNode, parentAttributeName?: string) {
    super(undefined, parent, parentAttributeName);
    this.__meta.typeName = "furo.Fieldoption";
    this.__meta.description = "Fieldoption Metas for a field";

    this.__meta.nodeFields = [
      {
        fieldName: "flags",
        protoName: "flags",
        FieldConstructor: STRING,
        constraints: {},
        description: 'Add flags for your field. This can be something like "searchable". \n //The flags can be used by generators, ui components,...',
      },
      {
        fieldName: "list",
        protoName: "list",
        FieldConstructor: ANY,
        constraints: {},
        description: "a list with options, use furo.optionitem or your own",
      },
    ];

    // Initialize the fields
    // ---------------------

    /**
     *  Add flags for your field. This can be something like "searchable".
     *  //The flags can be used by generators, ui components,...
     **/
    this._flags = new ARRAY<STRING, string>(undefined, this, "flags");

    /**
     *  a list with options, use furo.optionitem or your own
     **/
    this._list = new ANY(undefined, this, "list");

    // Set required fields
    [].forEach((fieldName) => {
      (this[fieldName as keyof Fieldoption] as FieldNode).__meta.required = true;
    });

    // Default values from openAPI annotations
    this.__defaultValues = {};

    // Initialize the fields with init data
    if (initData !== undefined) {
      this.__fromLiteral({ ...this.__defaultValues, ...initData });
    } else {
      this.__fromLiteral(this.__defaultValues);
    }

    // Set readonly fields after the init, so child nodes are readonly too
    [].forEach((fieldName) => {
      (this[fieldName as keyof Fieldoption] as FieldNode).__readonly = true;
    });

    this.__meta.isPristine = true;
  }

  /**
   *  Add flags for your field. This can be something like "searchable".
   *  //The flags can be used by generators, ui components,...
   * The getter receives the FieldNode
   **/
  public get flags(): ARRAY<STRING, string> {
    return this._flags;
  }

  /**
   * The setter receives `string[]`
   **/
  public set flags(v: string[]) {
    this.__TypeSetter(this._flags, v);
  }

  /**
   *  a list with options, use furo.optionitem or your own
   * The getter receives the FieldNode
   **/
  public get list(): ANY {
    return this._list;
  }

  /**
   * The setter receives `IAny`
   **/
  public set list(v: IAny) {
    this.__TypeSetter(this._list, v);
  }

  fromLiteral(data: IFieldoption): void {
    super.__fromLiteral(data);
  }

  toLiteral(): IFieldoption {
    return super.__toLiteral() as IFieldoption;
  }
}

Registry.register("furo.Fieldoption", Fieldoption);
