// Code generated by furo protoc-gen-open-models. DO NOT EDIT.
// protoc-gen-open-models version: ????

import { ANY, ARRAY, BOOLEAN, FieldNode, INT32, Registry, STRING, type IAny } from "@furo/open-models/dist/index";

import { FeatureToggle as FuroFeatureToggle, type IFeatureToggle as IFuroFeatureToggle, type TFeatureToggle as TFuroFeatureToggle } from "./FeatureToggle";

/**
 * @interface IMessageContainer
 *  The `MessageContainer` type defines a logical message model.
 */
export interface IMessageContainer {
  /**
   *  This field is for compatibility with google.rpc.Status.
   */
  code?: number;
  /**
   *  This field is for compatibility with google.rpc.Status.
   */
  message?: string;
  /**
   *  A list of messages that carry the message details. There is a common set of message types for APIs to use.
   */
  details?: IAny;
  /**
   *  Use this field to notify a success. Even when a action was successful, it can have information and warning states.
   */
  success?: boolean;
  /**
   *  A list of feature toggles
   */
  featureToggles?: IFuroFeatureToggle[];
}

/**
 * @interface TMessageContainer
 *  The `MessageContainer` type defines a logical message model.
 */
export interface TMessageContainer {
  /**
   *  This field is for compatibility with google.rpc.Status.
   */
  code?: number;
  /**
   *  This field is for compatibility with google.rpc.Status.
   */
  message?: string;
  /**
   *  A list of messages that carry the message details. There is a common set of message types for APIs to use.
   */
  details?: IAny;
  /**
   *  Use this field to notify a success. Even when a action was successful, it can have information and warning states.
   */
  success?: boolean;
  /**
   *  A list of feature toggles
   */
  feature_toggles?: TFuroFeatureToggle[];
}

/**
 * MessageContainer
 *  The `MessageContainer` type defines a logical message model.
 */
export class MessageContainer extends FieldNode {
  /**
   * This field is for compatibility with google.rpc.Status.
   **/
  private _code: INT32;

  /**
   * This field is for compatibility with google.rpc.Status.
   **/
  private _message: STRING;

  /**
   * A list of messages that carry the message details. There is a common set of message types for APIs to use.
   **/
  private _details: ANY;

  /**
   * Use this field to notify a success. Even when a action was successful, it can have information and warning states.
   **/
  private _success: BOOLEAN;

  /**
   * A list of feature toggles
   **/
  private _featureToggles: ARRAY<FuroFeatureToggle, IFuroFeatureToggle>;

  public __defaultValues: IMessageContainer;

  constructor(initData?: IMessageContainer, parent?: FieldNode, parentAttributeName?: string) {
    super(undefined, parent, parentAttributeName);
    this.__meta.typeName = "furo.MessageContainer";
    this.__meta.description = "MessageContainer The `MessageContainer` type defines a logical message model.";

    this.__meta.nodeFields = [
      {
        fieldName: "code",
        protoName: "code",
        FieldConstructor: INT32,
        constraints: {},
        description: "This field is for compatibility with google.rpc.Status.",
      },
      {
        fieldName: "message",
        protoName: "message",
        FieldConstructor: STRING,
        constraints: {},
        description: "This field is for compatibility with google.rpc.Status.",
      },
      {
        fieldName: "details",
        protoName: "details",
        FieldConstructor: ANY,
        constraints: {},
        description: "A list of messages that carry the message details. There is a common set of message types for APIs to use.",
      },
      {
        fieldName: "success",
        protoName: "success",
        FieldConstructor: BOOLEAN,
        constraints: {},
        description: "Use this field to notify a success. Even when a action was successful, it can have information and warning states.",
      },
      {
        fieldName: "featureToggles",
        protoName: "feature_toggles",
        FieldConstructor: FuroFeatureToggle,
        constraints: {},
        description: "A list of feature toggles",
      },
    ];

    // Initialize the fields
    // ---------------------

    /**
     *  This field is for compatibility with google.rpc.Status.
     **/
    this._code = new INT32(undefined, this, "code");

    /**
     *  This field is for compatibility with google.rpc.Status.
     **/
    this._message = new STRING(undefined, this, "message");

    /**
     *  A list of messages that carry the message details. There is a common set of message types for APIs to use.
     **/
    this._details = new ANY(undefined, this, "details");

    /**
     *  Use this field to notify a success. Even when a action was successful, it can have information and warning states.
     **/
    this._success = new BOOLEAN(undefined, this, "success");

    /**
     *  A list of feature toggles
     **/
    this._featureToggles = new ARRAY<FuroFeatureToggle, IFuroFeatureToggle>(undefined, this, "featureToggles");

    // Set required fields
    [].forEach((fieldName) => {
      (this[fieldName as keyof MessageContainer] as FieldNode).__meta.required = true;
    });

    // Default values from openAPI annotations
    this.__defaultValues = {};

    // Initialize the fields with init data
    if (initData !== undefined) {
      this.__fromLiteral({ ...this.__defaultValues, ...initData });
    } else {
      this.__fromLiteral(this.__defaultValues);
    }

    // Set readonly fields after the init, so child nodes are readonly too
    [].forEach((fieldName) => {
      (this[fieldName as keyof MessageContainer] as FieldNode).__readonly = true;
    });

    this.__meta.isPristine = true;
  }

  /**
   *  This field is for compatibility with google.rpc.Status.
   * The getter receives the FieldNode
   **/
  public get code(): INT32 {
    return this._code;
  }

  /**
   * The setter receives `number`
   **/
  public set code(v: number) {
    this.__PrimitivesSetter(this._code, v);
  }

  /**
   *  This field is for compatibility with google.rpc.Status.
   * The getter receives the FieldNode
   **/
  public get message(): STRING {
    return this._message;
  }

  /**
   * The setter receives `string`
   **/
  public set message(v: string) {
    this.__PrimitivesSetter(this._message, v);
  }

  /**
   *  A list of messages that carry the message details. There is a common set of message types for APIs to use.
   * The getter receives the FieldNode
   **/
  public get details(): ANY {
    return this._details;
  }

  /**
   * The setter receives `IAny`
   **/
  public set details(v: IAny) {
    this.__TypeSetter(this._details, v);
  }

  /**
   *  Use this field to notify a success. Even when a action was successful, it can have information and warning states.
   * The getter receives the FieldNode
   **/
  public get success(): BOOLEAN {
    return this._success;
  }

  /**
   * The setter receives `boolean`
   **/
  public set success(v: boolean) {
    this.__PrimitivesSetter(this._success, v);
  }

  /**
   *  A list of feature toggles
   * The getter receives the FieldNode
   **/
  public get featureToggles(): ARRAY<FuroFeatureToggle, IFuroFeatureToggle> {
    return this._featureToggles;
  }

  /**
   * The setter receives `IFuroFeatureToggle[]`
   **/
  public set featureToggles(v: IFuroFeatureToggle[]) {
    this.__TypeSetter(this._featureToggles, v);
  }

  fromLiteral(data: IMessageContainer): void {
    super.__fromLiteral(data);
  }

  toLiteral(): IMessageContainer {
    return super.__toLiteral() as IMessageContainer;
  }
}

Registry.register("furo.MessageContainer", MessageContainer);
