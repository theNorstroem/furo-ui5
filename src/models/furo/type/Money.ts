// Code generated by furo protoc-gen-open-models. DO NOT EDIT.
// protoc-gen-open-models version: ????

import { FieldNode, INT32, INT64, Registry, STRING } from "@furo/open-models/dist/index";

/**
 * @interface IMoney
 *  Represents an amount of money with its currency type.
 */
export interface IMoney {
  /**
   *  The 3-letter currency code defined in ISO 4217.
   */
  currencyCode?: string;
  /**
   *  The whole units of the amount.
   *   For example if `currencyCode` is `"USD"`, then 1 unit is one US dollar.
   */
  units?: string;
  /**
   *  Number of nano (10^-9) units of the amount.
   *   The value must be between -999,999,999 and +999,999,999 inclusive.
   *   If `units` is positive, `nanos` must be positive or zero.
   *   If `units` is zero, `nanos` can be positive, zero, or negative.
   *   If `units` is negative, `nanos` must be negative or zero.
   *   For example $-1.75 is represented as `units`=-1 and `nanos`=-750,000,000.
   */
  nanos?: number;
  /**
   *  String representation of the reference
   */
  displayName?: string;
}

/**
 * @interface TMoney
 *  Represents an amount of money with its currency type.
 */
export interface TMoney {
  /**
   *  The 3-letter currency code defined in ISO 4217.
   */
  currency_code?: string;
  /**
   *  The whole units of the amount.
   *   For example if `currencyCode` is `"USD"`, then 1 unit is one US dollar.
   */
  units?: string;
  /**
   *  Number of nano (10^-9) units of the amount.
   *   The value must be between -999,999,999 and +999,999,999 inclusive.
   *   If `units` is positive, `nanos` must be positive or zero.
   *   If `units` is zero, `nanos` can be positive, zero, or negative.
   *   If `units` is negative, `nanos` must be negative or zero.
   *   For example $-1.75 is represented as `units`=-1 and `nanos`=-750,000,000.
   */
  nanos?: number;
  /**
   *  String representation of the reference
   */
  display_name?: string;
}

/**
 * Money
 *  Represents an amount of money with its currency type.
 */
export class Money extends FieldNode {
  /**
   * The 3-letter currency code defined in ISO 4217.
   **/
  private _currencyCode: STRING;

  /**
   * The whole units of the amount.
   *  For example if `currencyCode` is `"USD"`, then 1 unit is one US dollar.
   **/
  private _units: INT64;

  /**
   * Number of nano (10^-9) units of the amount.
   *  The value must be between -999,999,999 and +999,999,999 inclusive.
   *  If `units` is positive, `nanos` must be positive or zero.
   *  If `units` is zero, `nanos` can be positive, zero, or negative.
   *  If `units` is negative, `nanos` must be negative or zero.
   *  For example $-1.75 is represented as `units`=-1 and `nanos`=-750,000,000.
   **/
  private _nanos: INT32;

  /**
   * String representation of the reference
   **/
  private _displayName: STRING;

  public __defaultValues: IMoney;

  constructor(initData?: IMoney, parent?: FieldNode, parentAttributeName?: string) {
    super(undefined, parent, parentAttributeName);
    this.__meta.typeName = "furo.type.Money";
    this.__meta.description = "Money Represents an amount of money with its currency type.";

    this.__meta.nodeFields = [
      {
        fieldName: "currencyCode",
        protoName: "currency_code",
        FieldConstructor: STRING,
        constraints: {},
        description: "The 3-letter currency code defined in ISO 4217.",
      },
      {
        fieldName: "units",
        protoName: "units",
        FieldConstructor: INT64,
        constraints: {},
        description: 'The whole units of the amount.\n  For example if `currencyCode` is `"USD"`, then 1 unit is one US dollar.',
      },
      {
        fieldName: "nanos",
        protoName: "nanos",
        FieldConstructor: INT32,
        constraints: {},
        description:
          "Number of nano (10^-9) units of the amount.\n  The value must be between -999,999,999 and +999,999,999 inclusive.\n  If `units` is positive, `nanos` must be positive or zero.\n  If `units` is zero, `nanos` can be positive, zero, or negative.\n  If `units` is negative, `nanos` must be negative or zero.\n  For example $-1.75 is represented as `units`=-1 and `nanos`=-750,000,000.",
      },
      {
        fieldName: "displayName",
        protoName: "display_name",
        FieldConstructor: STRING,
        constraints: {},
        description: "String representation of the reference",
      },
    ];

    // Initialize the fields
    // ---------------------

    /**
     *  The 3-letter currency code defined in ISO 4217.
     **/
    this._currencyCode = new STRING(undefined, this, "currencyCode");

    /**
     *  The whole units of the amount.
     *   For example if `currencyCode` is `"USD"`, then 1 unit is one US dollar.
     **/
    this._units = new INT64(undefined, this, "units");

    /**
     *  Number of nano (10^-9) units of the amount.
     *   The value must be between -999,999,999 and +999,999,999 inclusive.
     *   If `units` is positive, `nanos` must be positive or zero.
     *   If `units` is zero, `nanos` can be positive, zero, or negative.
     *   If `units` is negative, `nanos` must be negative or zero.
     *   For example $-1.75 is represented as `units`=-1 and `nanos`=-750,000,000.
     **/
    this._nanos = new INT32(undefined, this, "nanos");

    /**
     *  String representation of the reference
     **/
    this._displayName = new STRING(undefined, this, "displayName");

    // Set required fields
    [].forEach(fieldName => {
      (this[fieldName as keyof Money] as FieldNode).__meta.required = true;
    });

    // Default values from openAPI annotations
    this.__defaultValues = {};

    // Initialize the fields with init data
    if (initData !== undefined) {
      this.__fromLiteral({ ...this.__defaultValues, ...initData });
    } else {
      this.__fromLiteral(this.__defaultValues);
    }

    // Set readonly fields after the init, so child nodes are readonly too
    [].forEach(fieldName => {
      (this[fieldName as keyof Money] as FieldNode).__readonly = true;
    });

    this.__meta.isPristine = true;
  }

  /**
   *  The 3-letter currency code defined in ISO 4217.
   * The getter receives the FieldNode
   **/
  public get currencyCode(): STRING {
    return this._currencyCode;
  }

  /**
   * The setter receives `string`
   **/
  public set currencyCode(v: string) {
    this.__PrimitivesSetter(this._currencyCode, v);
  }

  /**
   *  The whole units of the amount.
   *   For example if `currencyCode` is `"USD"`, then 1 unit is one US dollar.
   * The getter receives the FieldNode
   **/
  public get units(): INT64 {
    return this._units;
  }

  /**
   * The setter receives `bigint`
   **/
  public set units(v: bigint) {
    this.__PrimitivesSetter(this._units, v);
  }

  /**
   *  Number of nano (10^-9) units of the amount.
   *   The value must be between -999,999,999 and +999,999,999 inclusive.
   *   If `units` is positive, `nanos` must be positive or zero.
   *   If `units` is zero, `nanos` can be positive, zero, or negative.
   *   If `units` is negative, `nanos` must be negative or zero.
   *   For example $-1.75 is represented as `units`=-1 and `nanos`=-750,000,000.
   * The getter receives the FieldNode
   **/
  public get nanos(): INT32 {
    return this._nanos;
  }

  /**
   * The setter receives `number`
   **/
  public set nanos(v: number) {
    this.__PrimitivesSetter(this._nanos, v);
  }

  /**
   *  String representation of the reference
   * The getter receives the FieldNode
   **/
  public get displayName(): STRING {
    return this._displayName;
  }

  /**
   * The setter receives `string`
   **/
  public set displayName(v: string) {
    this.__PrimitivesSetter(this._displayName, v);
  }

  fromLiteral(data: IMoney): void {
    super.__fromLiteral(data);
  }

  toLiteral(): IMoney {
    return super.__toLiteral() as IMoney;
  }
}

Registry.register("furo.type.Money", Money);
