- enum: |-
    google.protobuf.NullValue #`NullValue` is a singleton enumeration to represent the null value for the
     `Value` type union.

      The JSON representation for `NullValue` is JSON `null`.
  values:
    NULL_VALUE: 0
  target: ENUM_struct.proto
